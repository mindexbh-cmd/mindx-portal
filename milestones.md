# Milestones — Mindex Portal

قائمة مستخلصة من قراءة الكود وسجل الـ commits في `2026-04-19`. ليست مواصفات صادرة عن المستخدم، بل تجميع للمشاهدات والمهام المقترحة. حدّث أو احذف ما لم يعد ينطبق.

## أُنجز مؤخرًا (من git log)

- [x] إضافة أعمدة `paid1..paid12` (المبلغ المدفوع) إلى جدول `taqseet` في DB/API/HTML/JS. — `3c431bd`, `85f15ed`
- [x] مزامنة المبلغ المدفوع من `student_payments` إلى `taqseet` عند حفظ دفعة. — `85f15ed`
- [x] إعادة تصميم مودال متابعة الدفع: إبقاء عمود القسط الأول فقط، تسمية "نوع الأقساط"، تعبئة السعر تلقائيًا من `taqseet`. — `4ca2a67`, `b40767f`, `493583c`
- [x] إصلاح النصوص العربية المُعطوبة (mojibake) في JS داخل مودال الدفع باستخدام escapes من نوع `\uXXXX`. — `74b87ac`, `950c280`, `5bbf720`
- [x] منطق upsert في الحضور (لا تكرار لنفس الطالب/التاريخ/المجموعة) وإزالة زر الإضافة اليدوي. — `96789b8`
- [x] مودال حذف الجداول من صفحة قاعدة البيانات. — `c1a61f7` + سلسلة إصلاحات.
- [x] تصحيح تنسيق RTL ومحاذاة شريط التمرير. — `200e2f3`, `fdede7b`

## مهام مفتوحة / مشاهدات من الكود

### أمن
- [ ] هاش كلمات السر عبر `SHA-256` بدون salt في `hp()` (`app.py:21`). استبدالها بـ `hashlib.scrypt` أو `werkzeug.security.generate_password_hash`. يتطلب خطة ترحيل للمستخدمين الموجودين.
- [ ] `SECRET_KEY` الافتراضي `"mindx2026secret"` مكتوب في المصدر (`app.py:6`) — آمن على Render لأنه يُولَّد هناك، لكن يجب عدم تشغيل الإنتاج بدون ضبط المتغير.
- [ ] أدوار المستخدمين (`admin`, `reception`, `teacher`, ...) محفوظة في جدول `users` لكنها غير مُفعَّلة في الراوتات — كل مستخدم مسجَّل دخوله يستطيع الوصول لكل الـ API. إضافة decorator `@role_required(...)` أو فحص داخل كل endpoint.

### شاشة تسجيل الدخول
- [ ] أزرار الأدوار في `LOGIN_HTML` و `login.html` تعرض مستخدمين غير مُنشَئين في `init_db()`: `students/students123`, `curriculum/curriculum123`, `parent/parent123`, `zahraa/zahraa123`, `kauthar/kauthar123`, `media/media123`. إمّا إنشاؤهم في الـ seed، أو إزالة الأزرار.
- [ ] ملف `login.html` منفصل في الجذر غير مستخدم من قِبَل التطبيق (الراوت `/` يعيد `LOGIN_HTML` من داخل `app.py`). تحديد: إمّا حذفه أو استعماله كمصدر مفرد للحقيقة.
- [ ] `app.html` محتواه placeholder فقط — إمّا حذفه أو إكماله.

### Schema & migration
- [ ] مسار الـ schema مزدوج (`init_db()` مقابل كتلة migration في `else`) وهذا هشّ: إضافة أي عمود/جدول تتطلّب لمس موضعين. المقترح: توحيد النسختين في دالة `migrate()` واحدة مبنية على `PRAGMA table_info` فقط.
- [ ] لا يوجد نظام إصدارات للـ DB (`user_version`). إضافته سيسهّل تتبع الهجرات لاحقًا.
- [ ] أسماء أعمدة مُركَّبة عبر concatenation في `UPDATE taqseet SET "+paid_col+"=?` و `ALTER TABLE ... ADD COLUMN` (`app.py:4081`, `app.py:262`, `app.py:299`). المدخلات مضبوطة داخليًا لكن يُفضَّل whitelist صريحة.

### هيكلة الكود
- [ ] `app.py` بحجم ~4100 سطر ويحتوي 5 قوالب HTML كـ string constants. تقسيم إلى modules (`routes/`, `templates/`) أو نقل القوالب إلى ملفات Jinja سيقلّل المخاطر عند التعديل.
- [ ] تكرار الـ Arabic escapes (`&#x...;` في HTML، `\uXXXX` في JS) يجعل القراءة والتعديل صعبًا. الحل طويل الأمد: قالب Jinja + ملف translations أو استخدام UTF-8 خام مع ضبط encoding في كل خطوة في CI/Deploy.
- [ ] لا يوجد فصل واضح بين طبقة الـ data access وطبقة الراوتات — كل راوت يكتب SQL مباشرة.

### جودة
- [ ] لا يوجد linter (`ruff`/`flake8`) ولا type checker ولا اختبارات. إضافة حتى smoke tests على الـ API endpoints (`/api/students`, `/api/payments/...`) ستكشف الانحدارات.
- [ ] لا يوجد logging (بخلاف الافتراضي في Flask/Gunicorn). إضافة log على عمليات الدفع والحذف مفيد للتدقيق.

## مقترحات وظيفية محتملة

- [ ] تقارير مجمّعة للمدفوعات حسب المجموعة/الشهر (البيانات موجودة في `student_payments` و `taqseet`).
- [ ] تصدير CSV/Excel لجداول الطلاب والحضور والأقساط.
- [ ] إشعارات/تنبيهات للأقساط المتأخرة (يمكن حسابها من `taqseet.dateN` و `paidN`).

---

**ملاحظة:** هذا الملف مرجع داخلي فقط. المهام التي يطلبها المستخدم مباشرةً تبقى المصدر الموثوق؛ استخدم هذه القائمة كنقطة انطلاق وليس كـ roadmap رسمي.
